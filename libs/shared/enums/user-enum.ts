
export enum UserStatus {
    Active  = 'active',
    Deleted = 'deleted',
    Blocked = 'blocked',
}

export enum UserRole {
    USER = 'USER',
    ADMIN = 'ADMIN',
    OPERATOR = 'OPERATOR',
    AUDITOR = 'AUDITOR',
}